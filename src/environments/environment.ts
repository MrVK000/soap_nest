export const environment = {
  production: false,
  /**
   * Local/development API base URL.
   * Update this if your backend dev URL changes.
   */
  apiBaseUrl: 'http://localhost:5000/api/v1/',
  serverUrl: 'http://localhost:5000',
  /**
   * Razorpay public key for development/sandbox.
   * This is a PUBLIC key (not a secret). Use the live key only in production file.
   */
  razorpayKey: 'rzp_test_change_me_in_dev',
};

